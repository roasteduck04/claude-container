<?xml version="1.0" encoding="utf-8"?>
<!-- Secondary rail affordance (settings): same squircle, no accent fill, so it
     reads as chrome rather than as another container. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="@dimen/badge_radius" />
    <solid android:color="@color/badge_muted" />
</shape>
